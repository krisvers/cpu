#include <stdio.h>
#include <stdint.h>
#include <string.h>
#include <time.h>

#define CODE_LENGTH 256

uint8_t memory[8192];

uint8_t code[CODE_LENGTH] = {
	
};

int main() {
	memcpy(memory, code, CODE_LENGHT);

	while (1) {
		
	}
}
